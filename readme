PARA COMPILAR NO HOST:

Talvez seja necessario instalar as libs 32bits:
ubuntu:
sudo apt-get install libc6-dev-i386

Compilar a libcthread:
Basta rodar o make no diretorio raiz:
$ make

Para rodar os testes:
EXECUTAR dentro do diretorio /TESTES:
$ make
$ ./main

